import React from "react";
import { Card, Button, Text, View } from "react-native";

export default ({ navigation }) => (
    <View style={{ paddingVertical: 20 }}>
        <Text>Teste</Text>
    </View>
);